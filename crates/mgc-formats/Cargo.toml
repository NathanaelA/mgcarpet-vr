[package]
name = "mgc-formats"
description = "Baked level-package format shared between the importer and the engine"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
